services:
  app:
    build: .
    ports:
      - "8080:8080"
  db:
    image: "postgres:latest"
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: freelance_db
    ports:
      - "5432:5432"

